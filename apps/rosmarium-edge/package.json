{
  "name": "@orchestrator.dev/edge",
  "version": "3.2.0",
  "private": true,
  "type": "module",
  "description": "Rosmarium COS Edge Runtime — lightweight content delivery and personalization for Cloudflare Workers / Fastly Compute",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "hono": "^4.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240405.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0",
    "wrangler": "^3.48.0"
  }
}
