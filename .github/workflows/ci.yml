name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:

  server:
    name: rosmarium-server
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: rosmarium
          POSTGRES_PASSWORD: rosmarium
          POSTGRES_DB: rosmarium_test
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports: ['6379:6379']
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22', cache: 'pnpm' }
      - run: pnpm install --frozen-lockfile
      - name: Enable pgvector
        run: |
          PGPASSWORD=rosmarium psql -h localhost -U rosmarium -d rosmarium_test \
            -c "CREATE EXTENSION IF NOT EXISTS vector;"
      - name: Typecheck
        run: pnpm --filter @rosmarium-cos/server typecheck
      - name: Lint
        run: pnpm --filter @rosmarium-cos/server lint
      - name: Test
        run: pnpm --filter @rosmarium-cos/server test
        env:
          DATABASE_URL: postgresql://rosmarium:rosmarium@localhost:5432/rosmarium_test
          REDIS_URL: redis://localhost:6379
          SESSION_SECRET: test-secret-minimum-32-characters-long
          NODE_ENV: test
          AI_WORKER_URL: http://localhost:8001
          AI_WORKER_SECRET: test-secret
          STORAGE_PROVIDER: minio
          STORAGE_ENDPOINT: http://localhost:9000
          STORAGE_BUCKET: rosmarium-test
          STORAGE_REGION: us-east-1
          STORAGE_ACCESS_KEY: test
          STORAGE_SECRET_KEY: test

  ai-worker:
    name: rosmarium-ai-worker
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: rosmarium
          POSTGRES_PASSWORD: rosmarium
          POSTGRES_DB: rosmarium_test
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports: ['6379:6379']
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - name: Install uv
        run: pip install uv
      - name: Install dependencies
        working-directory: apps/rosmarium-ai-worker
        run: uv sync --all-extras
      - name: Typecheck (mypy)
        working-directory: apps/rosmarium-ai-worker
        run: uv run mypy src/
      - name: Lint (ruff)
        working-directory: apps/rosmarium-ai-worker
        run: uv run ruff check src/ tests/
      - name: Test
        working-directory: apps/rosmarium-ai-worker
        run: uv run pytest --cov=src --cov-report=xml
        env:
          DATABASE_URL: postgresql://rosmarium:rosmarium@localhost:5432/rosmarium_test
          REDIS_URL: redis://localhost:6379
          EMBEDDING_PROVIDER: ollama
          EMBEDDING_MODEL: nomic-embed-text
          OLLAMA_BASE_URL: http://localhost:11434
          WORKER_SECRET: test-secret
          ENVIRONMENT: test

  admin:
    name: rosmarium-admin
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22', cache: 'pnpm' }
      - run: pnpm install --frozen-lockfile
      - name: Typecheck
        run: pnpm --filter @rosmarium-cos/admin typecheck
      - name: Lint
        run: pnpm --filter @rosmarium-cos/admin lint
      - name: Test
        run: pnpm --filter @rosmarium-cos/admin test
      - name: Build
        run: pnpm --filter @rosmarium-cos/admin build
